import { langOptions } from "../langOptions";
import React from "react";
import { clsx } from "clsx";

export default function Languages(props) {
  return <div className="languages">{props.languages}</div>;
}
